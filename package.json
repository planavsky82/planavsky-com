{
  "name": "planavsky-com-root",
  "version": "0.0.0",
  "scripts": {
    "firebase:login": "firebase login",
    "firebase:setup": "npm i -g firebase-tools && firebase init",
    "firebase:deploy": "./scripts/deploy.sh"
  },
  "private": true,
  "engines": {
    "node": "12"
  }
}
