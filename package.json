{
  "name": "planavsky-com-root",
  "version": "0.0.0",
  "scripts": {
    "firebase:login": "firebase login",
    "firebase:setup": "npm i -g firebase-tools && firebase init",
    "firebase:deploy": "npm run install:all && ./scripts/deploy.sh",
    "install:all": "cd angular && npm i && cd ../stencil-web-components && npm i && cd ../functions && npm i && cd ../blog && npm i && cd .."
  },
  "private": true,
  "engines": {
    "node": "12"
  }
}
